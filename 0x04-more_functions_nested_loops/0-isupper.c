#include "main.h"

/**
 * main - a program for uppercase char
 * @c: character to be checked
 * Return: 0 or 1
 */

int main(int hp)
{
	if (hp >= 'A' && hp <= 'Z')
		return (1);
	else
		return (0);
}
