#include <stdio.h>

/**
 *  main - program that prints the alphabets
 *  Return: always (success)
 */

int main(void)

        {
                char i;

                for (i = 'a'; i <= 'z'; i++)
                putchar(i);
                putchar('\n');
                return (0);

        }
~                                
