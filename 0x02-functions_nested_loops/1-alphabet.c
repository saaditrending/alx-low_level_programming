#include <stdio.h>
#include <main.h>

/**
 * main - a program prints the alphabets
 * Return: Alwas (Success)
 */

int main(void)

{
	char h;

	for (h = 'a'; h <= 'z'; h++)
	putchar(h);
	putchar('\n');
	return (0);

}
