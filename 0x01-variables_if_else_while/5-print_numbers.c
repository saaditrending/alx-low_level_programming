#include <stdio.h>

/**
 * main - prints all single numbers of base 10 from 0
 * Return: (0)
 */
int main(void)
{
	int hf;

	for (hf = 0; hf <= 9; hf++)
		printf("%d", hf);
	printf("\n");

	return (0);
}
